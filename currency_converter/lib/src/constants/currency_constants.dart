import 'package:currency_converter/src/enum/currency_enum.dart';

class CurrencyConstants {
  CurrencyConstants._();

  static const defaultLeftCurrency = Currency.dollar;
  static const defaultRightCurrency = Currency.real;
}
